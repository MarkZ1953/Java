package UCC.Momento_1;
import javax.swing.JOptionPane;

public class Operaciones_Matematicas {
    public static void main(String[] args) {

        int opcion = Integer.parseInt(JOptionPane.showInputDialog(;null, "Seleccione una figura (con el indice) : "));

        switch (opcion) {
            case 1:
                // Cuadrado
                int opcion = Integer.parseInt(JOptionPane.showInputDialog(;null, "Seleccione una figura (con el indice) : "));
                int opcion = Integer.parseInt(JOptionPane.showInputDialog(;null, "Seleccione una figura (con el indice) : "));
                calculo = l * a
            break;

            case 2:
                // Rectangulo
                int opcion = Integer.parseInt(JOptionPane.showInputDialog(;null, "Seleccione una figura (con el indice) : "));
                int opcion = Integer.parseInt(JOptionPane.showInputDialog(;null, "Seleccione una figura (con el indice) : "));
                calculo = b * a
            break;
            case 3:
            // Triangulo
                int opcion = Integer.parseInt(JOptionPane.showInputDialog(;null, "Seleccione una figura (con el indice) : "));
                int opcion = Integer.parseInt(JOptionPane.showInputDialog(;null, "Seleccione una figura (con el indice) : "));
                calculo = (b * a)/2
            break;
            case 4:
                int opcion = Integer.parseInt(JOptionPane.showInputDialog(;null, "Seleccione una figura (con el indice) : "));
                int opcion = Integer.parseInt(JOptionPane.showInputDialog(;null, "Seleccione una figura (con el indice) : "));
                // Circulo
                calculo = 3.1416 * r^2
            break;
        }
    }
}
